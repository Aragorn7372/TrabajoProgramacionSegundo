meta {
  name: Delete Product
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/productos/{{productoId}}
  auth: bearer
}

auth:bearer {
  token: {{authAdminToken}}
}

assert {
  res.status: eq 200
}
