meta {
  name: Delete Category
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/categories/{{categoryId}}
  auth: bearer
}

auth:bearer {
  token: {{authAdminToken}}
}

assert {
  res.status: eq 200
}
