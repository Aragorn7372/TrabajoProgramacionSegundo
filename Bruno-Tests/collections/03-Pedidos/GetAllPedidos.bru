meta {
  name: Get All Pedidos
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/pedidos?page=0&size=10&sortBy=id&direction=asc
  auth: bearer
}

auth:bearer {
  token: {{authAdminToken}}
}

assert {
  res.status: eq 200
  res.body.content: isArray
}
